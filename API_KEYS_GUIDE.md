# دليل مفاتيح API وإعدادات الذكاء الاصطناعي (Gemini API) - تطبيق مسار

تم إعداد هذا الملف لحفظ بيانات المفاتيح وتوضيح كيفية تغييرها والتحكم بها بسهولة في أي وقت.

---

## 🔑 1. إدارة مفتاح Gemini API بأمان

لحماية مشروعك عند الرفع إلى GitHub ولتجنب حظر الرفع بواسطة **GitHub Secret Protection**:
- **لا تضع مفاتيحك السرية داخل ملفات الكود المرفوعة**.
- أدخل مفتاحك بأمان عبر لوحة الأسرار **Secrets Panel** أو مباشرة من داخل واجهة التطبيق في شاشة **الإعدادات**.

---

## ⚙️ 2. من أين يمكنك تغيير مفتاح API؟ (4 طرق مختلفة)

### الطريقة الأولى: من داخل التطبيق نفسه (بدون لمس الكود) 📱
1. افتح تطبيق **مسار**.
2. انتقل إلى شاشة **«الإعدادات» (Settings)** أو من شاشة **«اسأل مسار» (Advisor)**.
3. ستجد حقلاً مخصصاً لـ **«مفتاح Gemini API»**.
4. الصق مفتاحك الجديد واضغط **«حفظ»**، وسيستخدمه التطبيق فوراً.

---

### الطريقة الثانية: من ملف البيئة `.env` 📄
- **مسار الملف:** `/.env` (في المجلد الرئيسي للمشروع).
- قم بكتابة المفتاح بالشكل التالي:
```env
GEMINI_API_KEY=مفتاحك_الجديد_هنا
```
- عند بناء التطبيق، سيقوم نظام Gradle بقراءته تلقائياً وحقنه عبر `BuildConfig.GEMINI_API_KEY`.

---

### الطريقة الثالثة: من لوحة الأسرار في Google AI Studio (Secrets Panel) 🔒
1. في شريط الأدوات العلوي أو الجانبي في منصة AI Studio، افتح **Secrets** أو **Settings**.
2. أضف سر جديد بالاسم: `GEMINI_API_KEY`.
3. القيمة: ضع مفتاحك الشخصي الجديد.

---

### الطريقة الرابعة: من الكود المصدري في Kotlin 💻
إذا أردت تعديل المفتاح الافتراضي المدمج مباشرة في الكود:
- **الملف:**
  `/app/src/main/java/com/example/data/remote/GeminiService.kt`
- **السطر (حوالي السطر 60):**
```kotlin
private const val DEFAULT_FALLBACK_API_KEY = "ضع_مفتاحك_الجديد_هنا"
```

---

## 🌐 3. حل مشكلة الرفع إلى GitHub (Push Error)

عند ظهور رسالة:
> `Failed to push commit to GitHub: Request contains an invalid argument.`

**الأسباب والحلول السريعة:**
1. **اسم المستودع (Repository Name):**
   - تأكد أن اسم المستودع بالإنجليزية وبدون مسافات أو رموز خاصة (مثال صحيح: `masar-app` أو `masar-fintech`).
2. **صلاحيات حساب GitHub (Permissions):**
   - قم بإلغاء ربط الحساب وإعادة تسجيل الدخول (Re-authorize GitHub) من إعدادات المنصة لمنح صلاحية إنشاء المستودعات.
3. **تجنب تضارب الملفات:**
   - عند إنشاء مستودع جديد على GitHub لا تضع علامة على "Initialize with README" حتى لا يحدث تعارض في الفرع الرئيسي (`main`).
4. **الحل البديل الفوري (تنزيل ZIP):**
   - يمكنك دائماً النقر على القائمة الثلاثية ⋮ أعلى اليمين ثم اختيار **Export / Download as ZIP** ورفع المشروع عبر GitHub Desktop أو Git CLI على جهازك بسهولة تامة.
